import { writable, type Writable } from "svelte/store";

export const bronContainerIndex: Writable<number> = writable(null);
export const steenIndex: Writable<number> = writable(null);
export const doelContainerIndex: Writable<number> = writable(null);
